datatable(mtcars,
          extensions = "Buttons",
          options = list(
                         dom="Bfrtip",
                         buttons=c("copy","print","csv")
                        )
)